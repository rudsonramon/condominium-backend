const Query = require('./resolvers/query')
const Block = require('./resolvers/block')
const Apartment = require('./resolvers/apartment')
//const Mutation = require('./resolvers/mutation')

module.exports = {
  Query,
  Block,
  Apartment,
};